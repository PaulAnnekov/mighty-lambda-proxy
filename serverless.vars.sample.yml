usagePlan:
  quota:
    limit: 1000000
    period: MONTH

cors:
  # 'origins' property is currently not supported. 
  origin: 'https://my-only-allowed-origin.example'
  allowCredentials: true
  headers:
    - Content-Type
    - Authorization
    - X-My-Unique-Header

proxyTimeout: 5000
